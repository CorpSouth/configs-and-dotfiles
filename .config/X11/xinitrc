#!/bin/sh

# Pipewire

/usr/bin/pipewire &
/usr/bin/pipewire-pulse &
/usr/bin/wireplumber &

# Xresources
xrdb -merge "$XRESOURCES" &

# Start session

exec dbus-launch --exit-with-session --sh-syntax i3
