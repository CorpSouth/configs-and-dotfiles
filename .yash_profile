######################
#Default Applications#
######################

export BROWSER='brave-browser'
export CALCULATOR='xcalc'
export EDITOR='nvim'
export FILEPICKER='filepicker.sh'
export MAIL='thunderbird'
export MIXER='pulsemixer'
export OFFICE='onlyoffice-desktopeditors'
export PAGER='nvim +Man!'
export PLAYER='mpv --player-operation-mode=pseudo-gui'
export TASKMANAGER='btop'
export TERMEXEC='uxterm -e'
export TERMINAL='uxterm'

##########################
#Organize XDG Directories#
##########################

# Cache
export XDG_CACHE_HOME="$HOME"/.cache

# Config
export XDG_CONFIG_HOME="$HOME"/.config

# Data
export XDG_DATA_HOME="$HOME"/.local/share

# X11
export ALTUSERXSESSION="$XDG_CACHE_HOME"/X11/Xsession
export ERRFILE="$XDG_CACHE_HOME/X11"/xsession-errors
export USERXSESSION="$XDG_CACHE_HOME"/X11/xsession
export USERXSESSIONRC="$XDG_CACHE_HOME"/X11/xsessionrc
export XINITRC="$XDG_CONFIG_HOME"/X11/xinitrc
export XRESOURCES="$XDG_CONFIG_HOME"/X11/xresources

# WM
export CWMRC="$XDG_CONFIG_HOME"/cwm/.cwmrc

######
#Path#
######

if [ -d "$HOME/.bin" ] ;
  then PATH="$HOME/.bin:$PATH"
fi

if [ -d "$HOME/.local/bin" ] ;
  then PATH="$HOME/.local/bin:$PATH"
fi

############################
#Single-User Cursor Theming#
############################

export XCURSOR_PATH="${XCURSOR_PATH}":~/.local/share/icons

######################################
#Treat the TTY Like a Display Manager#
######################################

if [ -z "${DISPLAY}" ] && [ "${XDG_VTNR}" -eq 1 ]; then
  exec startx "$XINITRC"
fi
